{
  "name": "job-application-bot",
  "version": "1.0.0",
  "description": "Automated resume + cover letter generator with ATS scoring and self-contained HTML output",
  "main": "build.js",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "docx": "^8.5.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "notes": {
    "libreoffice": "PDF generation requires LibreOffice installed (headless). Install: https://www.libreoffice.org/download/. If not installed, DOCX files are still generated and the HTML summary shows download links for DOCX only."
  }
}
